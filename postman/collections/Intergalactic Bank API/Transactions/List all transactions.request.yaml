headers:
  - key: x-api-key
    value: "{{apiKey}}"
method: GET
order: 1000
queryParams:
  - key: fromAccountId
    value: "{{fromAccountId}}"
  - key: toAccountId
    value: "{{toAccountId}}"
  - key: createdAt
    value: "{{createdAt}}"
type: http
url: http://localhost:3000/api/v1/transactions?fromAccountId={{fromAccountId}}&toAccountId={{toAccountId}}&createdAt={{createdAt}}
